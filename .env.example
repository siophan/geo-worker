# 智象 GEO 采集 worker 配置样例。复制为 .env 并填写,或直接 export 到环境。

# 服务端基址(末尾不带斜杠)。默认已写死生产地址 https://www.eleai.cc,
# 生产 worker 机【无需配置本项】;仅本地开发联调时取消注释覆盖:
# GEO_SERVER_URL=http://127.0.0.1:9102
# 与服务端 settings.collector_shared_secret 完全一致
GEO_COLLECTOR_TOKEN=replace-with-shared-secret

# 本机能服务的引擎(逗号分隔)。可选值:deepseek / doubao / kimi / wenxin / tongyi
GEO_WORKER_CAPABILITIES=deepseek

# ── 采集浏览器:只支持 CDP 挂真 Chrome(已删除 bundled/Chrome for Testing 兜底)──────
# worker 通过 CDP 挂到你手动起的真 Google Chrome 上驱动:指纹最真、复用真实已登录 profile、
# 免注入登录态,反风控最强。缺 CDP 配置 worker 会在启动时直接报错退出,绝不自弹浏览器。
#
#   1) 本机手动起一个专用 profile 的真 Chrome(带远程调试端口),并保持常驻:
#      "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
#        --remote-debugging-port=9222 --user-data-dir="$HOME/geo-chrome-profile"
#   2) 在这个 Chrome 里手动登录各引擎(chat.deepseek.com 等),登录态留在该 profile。
#   3) 配 CDP 地址(下面二选一)。
#
# 【推荐】全局 CDP:所有引擎共用同一个真 Chrome,一处配置搞定。
GEO_WORKER_CDP_URL=http://127.0.0.1:9222
#
# 【可选】按引擎覆盖:某引擎要挂到不同 Chrome/端口时,在它的 _CONFIG 里给 cdp_url
# (优先于全局 GEO_WORKER_CDP_URL)。<KEY> 为引擎大写名:
#   DEEPSEEK / DOUBAO / KIMI / WENXIN / TONGYI。
# GEO_ENGINE_DEEPSEEK_CONFIG='{"cdp_url":"http://127.0.0.1:9223"}'

# ── 可选:GEO_ENGINE_<KEY>_CONFIG(JSON,热覆盖该引擎参数,免改代码)───────
# ⚠️ JSON 值必须用【单引号】整段裹住!否则 `source .env` 时 shell 会吞掉里面的双引号,
#    值变成 {cdp_url:http://...} 不是合法 JSON,会被 worker 忽略(日志:忽略非法 ..._CONFIG)。
#    另:cdp_url 必须是【字符串】(带引号);写成数字 {"cdp_url":9222} 会在启动时被判无效并退出。
# 连接:cdp_url(见上)
# 选择器(新引擎默认为初始猜测,首次联调对照线上 DOM 校准):
#   url / input_selector / answer_selector / citation_selector /
#   submit("enter"|"button") / submit_selector / settle_ms / timeout_ms
# 反自动化:stealth(默认 True,注入 navigator.webdriver 等指纹抹除)
# 拟人节奏(一般用默认即可,需要时再调,单位 ms;概率类为 0~1):
#   type_delay_min/max(逐字延迟) / punct_pause_min/max(空格标点后额外停顿) /
#   hesitate_prob + hesitate_ms_min/max(打字中偶发"想一下") /
#   think_ms_min/max(思考停顿) / click_hold_min/max(按下保持) /
#   move_steps_min/max(鼠标轨迹步数) / poll_min/max(轮询) /
#   read_scroll_prob(等回答时"阅读"式滚动/挪鼠标的概率)
# 示例(CDP + 选择器校准合并;注意整段用单引号裹住):
# GEO_ENGINE_KIMI_CONFIG='{"cdp_url":"http://127.0.0.1:9222","input_selector":"textarea","answer_selector":".markdown"}'

# worker_id 持久化文件(默认当前目录 worker_id,重启保持同一身份)
# GEO_WORKER_ID_FILE=/Users/you/geo-worker/worker_id
